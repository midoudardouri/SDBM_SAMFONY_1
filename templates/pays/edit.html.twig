{% extends 'base.html.twig' %}

{% block title %}Edit Pays{% endblock %}

{% block body %}
<div class="container">
    <h1 class="my-5">Edit Pays</h1>

    <div class="row mb-3">
        <div class="col-12">
            {{ include('pays/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>

    <div class="row">
        <div class="col-6 d-flex justify-content-end">
            <a href="{{ path('app_pays_index') }}" class="btn btn-warning me-2 bi bi-backspace-fill">Back to list</a>
        </div>
        <div class="col-6 d-flex justify-content-start">
            {{ include('pays/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
